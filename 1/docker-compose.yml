---

version: "3.9"
services:
  web:
    container_name: "web"
    build: .\nginx
    ports:
      - "9000:80"
    environment:
      WAIT_HOSTS: app:3000

  app:
    container_name: "app"
    build: .\app

    